---
import { Font } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import Icon from '../components/Icon.astro';
import { siteName, pageTitle, twitterHandle, authorName } from '../support/site';
import { resolveSocialImage } from '../support/social-image';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  socialImage?: ImageMetadata;
  socialImageAlt?: string;
  type?: 'website' | 'article';
  pubDate?: Date;
  updatedDate?: Date;
  robots?: string;
  hasMarkdown?: boolean;
}

const {
  title,
  description,
  socialImage,
  socialImageAlt,
  type = 'website',
  pubDate,
  updatedDate,
  robots,
  hasMarkdown = true,
} = Astro.props;
const canonicalSiteUrl = Astro.site!.href;
const currentPath = Astro.url.pathname;
const canonicalUrl = new URL(currentPath, canonicalSiteUrl).href;
const cleanPath = currentPath.replace(/\/+$/, '') || '/';
const markdownPath = cleanPath === '/' ? '/index.md' : `${cleanPath}/index.md`;
const fullTitle = pageTitle(title);
const { url: ogImage, width: ogImageWidth, height: ogImageHeight } = await resolveSocialImage(
  socialImage,
  canonicalSiteUrl,
);

function isActive(path: string) {
  if (path === '/') return currentPath === '/';
  return currentPath.startsWith(path);
}

const navLinks = [
  { href: '/',             label: 'Home' },
  { href: '/blog/',        label: 'Blog' },
  { href: '/about/',       label: 'About' },
  { href: '/collections/', label: 'Collections' },
  { href: '/resume/',      label: 'Hire Me' },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />

    <!-- Primary meta -->
    <title>{fullTitle}</title>
    {description && <meta name="description" content={description} />}
    {robots && <meta name="robots" content={robots} />}
    <link rel="canonical" href={canonicalUrl} />
    {hasMarkdown && <link rel="alternate" type="text/markdown" href={markdownPath} />}
    <link rel="describedby" href="/llms.txt" />

    <!-- Icons -->
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" href="/favicon-32x32.png" type="image/png" sizes="32x32" />
    <link rel="icon" href="/favicon-16x16.png" type="image/png" sizes="16x16" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />

    <!-- Open Graph -->
    <meta property="og:site_name" content={siteName} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:type" content={type} />
    <meta property="og:locale" content="en_US" />
    {description && <meta property="og:description" content={description} />}
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content={String(ogImageWidth)} />
    <meta property="og:image:height" content={String(ogImageHeight)} />
    {socialImageAlt && <meta property="og:image:alt" content={socialImageAlt} />}
    {
      type === 'article' && pubDate && (
        <meta property="article:published_time" content={pubDate.toISOString()} />
      )
    }
    {
      type === 'article' && updatedDate && (
        <meta property="article:modified_time" content={updatedDate.toISOString()} />
      )
    }

    <!-- Twitter / X -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content={twitterHandle} />
    <meta name="twitter:creator" content={twitterHandle} />
    <meta name="twitter:title" content={fullTitle} />
    {description && <meta name="twitter:description" content={description} />}
    <meta name="twitter:image" content={ogImage} />
    {socialImageAlt && <meta name="twitter:image:alt" content={socialImageAlt} />}

    <!-- Feeds -->
    <link rel="alternate" type="application/rss+xml" title={siteName} href="/rss.xml" />

    <!-- Fonts: self-hosted via Astro Fonts API -->
    <Font cssVariable="--font-bricolage" preload />
    <Font cssVariable="--font-lexend" />
    <Font cssVariable="--font-geist" />

    <!-- Per-page head additions (e.g. JSON-LD, hero preload) -->
    <slot name="head" />

    <!-- Dark mode: apply class before paint to avoid flash -->
    <script is:inline src="/theme.js"></script>

    <!-- Umami analytics -->
    <script is:inline defer src="https://cloud.umami.is/script.js" data-website-id="5262f0b3-3bd0-4b9f-8d21-aa555515a81d"></script>
  </head>
  <body
    class="bg-surface dark:bg-bg-dark text-on-surface dark:text-inverse-on-surface font-body-md antialiased overflow-x-clip relative min-h-screen flex flex-col"
  >
    <a href="#main-content" class="skip-link">Skip to content</a>

    <!-- Header / Nav -->
    <header class="site-header">
      <nav
        class="nav-slab"
        aria-label="Main"
      >
        <a
          href="/"
          class="slab-mark nav-link"
        >
          JEWEI.NET
        </a>

        <ul class="slab-nav hidden md:flex">
          {
            navLinks.map(({ href, label }) => (
              <li>
                <a
                  href={href}
                  class:list={[
                    'nav-link',
                    isActive(href)
                      ? 'is-active'
                      : '',
                  ]}
                >
                  {label}
                </a>
              </li>
            ))
          }
        </ul>

        <div class="nav-actions">
          <button
            id="theme-toggle"
            type="button"
            aria-label="Toggle dark mode"
            class="nav-icon-btn"
          >
            <Icon name="light_mode" class="icon-light text-[20px]" />
            <Icon name="dark_mode" class="icon-dark text-[20px]" />
          </button>
          <button
            id="menu-toggle"
            type="button"
            aria-label="Toggle navigation menu"
            aria-controls="mobile-menu"
            aria-expanded="false"
            class="nav-icon-btn md:hidden"
          >
            <Icon name="menu" class="icon-menu-open text-[24px]" />
            <Icon name="close" class="icon-menu-close hidden text-[24px]" />
          </button>
        </div>
      </nav>

      <!-- Mobile menu (toggled by #menu-toggle) -->
      <div
        id="mobile-menu"
        class="mobile-nav hidden md:hidden"
      >
        <ul class="mobile-nav__list">
          {
            navLinks.map(({ href, label }) => (
              <li>
                <a
                  href={href}
                  class:list={[
                    'nav-link mobile-nav__link',
                    isActive(href)
                      ? 'is-active'
                      : '',
                  ]}
                >
                  {label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </header>

    <main
      id="main-content"
      class="site-main"
    >
      <slot />
    </main>

    <footer class="site-footer">
      <div class="footer-statement">
        <p class="footer-statement__line">
          <span class="footer-statement__row">
            <span>Build.</span>
            <span>Write.</span>
          </span>
          <span class="footer-statement__row">
            <span>Learn.</span>
            <strong>Share.</strong>
          </span>
        </p>
        <svg
          class="footer-loop"
          viewBox="0 0 420 190"
          role="img"
          aria-labelledby="footer-loop-title"
        >
          <title id="footer-loop-title">Build, write, learn, and share in a continuous loop.</title>
          <defs>
            <marker
              id="footer-loop-arrowhead"
              viewBox="0 0 10 10"
              refX="8"
              refY="5"
              markerWidth="6"
              markerHeight="6"
              orient="auto-start-reverse"
            >
              <path class="footer-loop__arrowhead" d="M 0 0 L 10 5 L 0 10 z"></path>
            </marker>
          </defs>
          <g class="footer-loop__arrows">
            <path class="footer-loop__arrow footer-loop__arrow--top" d="M 152 38 H 262"></path>
            <path class="footer-loop__arrow footer-loop__arrow--right" d="M 344 68 V 112"></path>
            <path class="footer-loop__arrow footer-loop__arrow--bottom" d="M 268 152 H 158"></path>
            <path class="footer-loop__arrow footer-loop__arrow--left" d="M 76 122 V 78"></path>
          </g>
          <g class="footer-loop__nodes">
            <g class="footer-loop__node">
              <rect x="12" y="12" width="128" height="52" rx="10"></rect>
              <text x="76" y="38">BUILD</text>
            </g>
            <g class="footer-loop__node">
              <rect x="280" y="12" width="128" height="52" rx="10"></rect>
              <text x="344" y="38">WRITE</text>
            </g>
            <g class="footer-loop__node">
              <rect x="280" y="126" width="128" height="52" rx="10"></rect>
              <text x="344" y="152">LEARN</text>
            </g>
            <g class="footer-loop__node footer-loop__node--share">
              <rect x="12" y="126" width="128" height="52" rx="10"></rect>
              <text x="76" y="152">SHARE</text>
            </g>
          </g>
        </svg>
      </div>
      <div class="footer-meta">
        <div class="footer-credit">
          <strong>JEWEI.NET</strong>
          <span>&copy; 2022–{new Date().getFullYear()} {authorName}</span>
        </div>
        <div class="footer-links">
        <a href="/contact/" class="footer-link">Contact</a>
        <a href="/privacy/" class="footer-link">Privacy</a>
        <a
          href="/rss.xml"
          class="footer-link"
        >
          <Icon name="rss_feed" class="text-[18px]" />
          RSS
        </a>
        <a
          href="https://github.com/sponsors/jewei"
          target="_blank"
          rel="noopener noreferrer"
          class="footer-link"
        >
          <Icon name="favorite" class="text-[18px]" />
          Sponsor<span class="sr-only"> (opens in new tab)</span>
        </a>
        <a
          href="https://x.com/jewei"
          rel="noopener noreferrer"
          target="_blank"
          class="footer-link"
        >
          <svg
            class="w-[18px] h-[18px] shrink-0"
            fill="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"
            ></path>
          </svg>
          Twitter/X<span class="sr-only"> (opens in new tab)</span>
        </a>
        <a
          href="https://github.com/jewei"
          rel="noopener noreferrer"
          target="_blank"
          class="footer-link"
        >
          <svg
            class="w-[18px] h-[18px] shrink-0"
            fill="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"
            ></path>
          </svg>
          GitHub<span class="sr-only"> (opens in new tab)</span>
        </a>
        </div>
      </div>
    </footer>

    <script is:inline src="/nav.js"></script>
  </body>
</html>
